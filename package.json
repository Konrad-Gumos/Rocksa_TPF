{
  "name": "rocksa",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "dev": "bun --filter @rocksa/web dev",
    "api": "bun --filter @rocksa/api dev",
    "db:generate": "bun --filter @rocksa/db generate",
    "db:migrate": "bun --filter @rocksa/db migrate",
    "db:seed": "bun --filter @rocksa/db seed",
    "build": "bun --filter='@rocksa/*' run build",
    "typecheck": "bun --filter='@rocksa/*' run typecheck",
    "test": "vitest run",
    "test:auth": "vitest run packages/auth/src/auth.e2e.test.ts",
    "test:watch": "vitest",
    "emulators:auth": "firebase emulators:start --only auth",
    "lint": "oxlint --config packages/config/oxlint.json .",
    "fmt": "oxfmt .",
    "fmt:check": "oxfmt --check .",
    "sync:editor": "bun scripts/sync-editor-settings.mjs",
    "postinstall": "bun scripts/sync-editor-settings.mjs"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@types/bun": "latest",
    "@types/node": "^22.9.0",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "jsdom": "^29.1.1",
    "oxfmt": "^0.54.0",
    "oxlint": "^0.15.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "packageManager": "bun@1.3.6"
}
