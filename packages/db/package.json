{
  "name": "@rocksa/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "echo 'no build (source package)'",
    "generate": "drizzle-kit generate",
    "migrate": "bun --env-file=../../.env.development --env-file=../../.env run src/migrate.ts",
    "seed": "bun --env-file=../../.env.development --env-file=../../.env run src/seed.ts"
  },
  "dependencies": {
    "drizzle-orm": "^0.36.0",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "drizzle-kit": "^0.28.0"
  }
}
